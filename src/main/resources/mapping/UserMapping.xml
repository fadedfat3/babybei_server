<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhumingbei.babybei_server.mapper.UserMapper">
    <resultMap id="UserMap" type="com.zhumingbei.babybei_server.entity.Users">
        <id property="id" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="lastPasswordChanged" column="last_password_changed"/>
        <collection property="roles" ofType="com.zhumingbei.babybei_server.entity.Roles">
            <id property="id" column="role_id"/>
            <result property="roleName" column="role_name"/>
            <result property="roleCode" column="role_code"/>
        </collection>
        <collection property="permissions" ofType="com.zhumingbei.babybei_server.entity.Permissions">
            <id property="id" column="permission_id"/>
            <result property="name" column="name"/>
            <result property="code" column="code"/>
        </collection>
    </resultMap>
    <select id="getUserByName" resultMap="UserMap">
        select user.id as user_id, username, password, last_password_changed, role.id as role_id, role_name, role_code, permission.id as permission_id, `name`, code
        from user
        left join user_role on user.id = user_role.user_id
        left join role on role.id = user_role.role_id
        left join role_permission on role_permission.role_id = role.id
        left join permission on permission.id = role_permission.permission_id
        where username = #{username};
    </select>

    <select id="getList" resultMap="UserMap">
        select user.id as user_id, username, password, last_password_changed, role.id as role_id, role_name, role_code, permission.id as permission_id, `name`, code
        from user
        left join user_role on user.id = user_role.user_id
        left join role on role.id = user_role.role_id
        left join role_permission on role_permission.role_id = role.id
        left join permission on permission.id = role_permission.permission_id;
    </select>

</mapper>